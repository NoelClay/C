
ex_01
창시자가 누구인지는 모르겠고, C언어는 Unix기반 응용프로그램을 개발하기 위해 만들어진 언어

ex_02
{} 스코프 안에 여러 줄이 써지는거다.

ex_03
함수를 정의하는 쪽에서 함수명 뒤에 괄호에 파라미터를 정의한다. 빈칸 일수도 있고, 여러개 일수도 있다.
이때 함수 호출자(caller)를 써서 피호출자(callee)를 깨우는데, 호출자에서 매개변수를 확실하게 정해줘야된다.
이때 매개변수가 함수 정의와 달라서, 피호출자가 못알아먹게 되면, 바로 에러 뜬다.

ex_04
빌드 단축키 f7 or ctrl+shift+b 
빌드를 하게 되면 벌어지는 일 : c소스 파일을 해석하여 기계어로 번역하는 일(컴파일)을 통해 obj 파일이 만들어지고,
obj파일을 해석하여(링크) 실행파일 exe를 만든다.

ex_05
#include <stdio.h>

int main()
{
    printf("010-1234-5678\n홍길동");
    

    return 0;
}
