1)
변수는 메모리에 올라가서 할당 된 값을 가지고 지지고 볶는 녀석. 변수의 이름으로 할당된 값을 반환할 수 있고, 저장할 수 있음.

2)
자료형은 데이터를 저장하는 방식, 불러오는 방식, 해석하는 방식을 유형에따라 구별지은거.

3)
변수를 이루는 세가지 요소는 아무래도.. 자료형이 있어야 얘를 얼만큼 할당할지 할당한걸 어떻게 해석할지 알거 같고,
변수명이 있어야, 사용할때 구분자로 사용할 수 있을거 같고,
나머지 세번째는 값아니면 메모리 주소인거 같은데... 
메모리 주소는 리터럴 상수와 구별되는 변수만의 특징으로 읽기 쓰기를 위해서는 메모리에 올라가 있어야 되서 그렇고,
값은 진짜 변수를 구성하는 요소 중 하나인데.. 메모리도 마찬가지고 음..

4)
둘 다 리터럴 상수인데, 10은 부호비트를 가지고 있는 정수형이고, 10U는 부호비트를 고려하지 않고 온전히 숫자만 쓰겠다는거

5)
ASCII

6)
변수의 메모리 주소